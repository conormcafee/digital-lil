<header class="flex items-center justify-center p-4 w-full">
  <nav class="flex items-center gap-5 [ bg-white bg-opacity-90 border-2 border-white rounded-[43px] px-4 py-1 ]">
    <a href="/">
      {% include "../../assets/svg/nav-logo.svg" %}
    </a>
    {% for link in navigation %}
      <a href="{{ link.href }}" class="font-medium text-secondary hover:text-accent text-sm md:text-base">{{ link.label }}</a>
    {% endfor %}
  </nav>
</header>
